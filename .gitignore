# Dependencies
# Ignore the node_modules directory where local dependencies are stored.
/node_modules/

# Build output
# Ignore the dist directory where compiled TypeScript files are stored.
/dist/

# Environment variables
# Ignore environment files, which may contain sensitive information.
# A .env.example file should be committed instead.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Uploads
# Ignore all files within the uploads directory, but keep the directory itself
# using a .gitkeep file. This prevents uploaded user content from being
# committed to the repository.
uploads/*
!uploads/.gitkeep

# Logs
# Ignore log files generated by the application or package managers.
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS generated files
# Ignore files generated by the operating system.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE directories
# Ignore configuration files and directories from common IDEs.
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Optional local configuration
# Ignore local npm configuration files.
.npmrc